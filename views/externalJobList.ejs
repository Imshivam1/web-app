<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>External Job List</title>
</head>
<body>
    <h1>External Job List</h1>
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Company</th>
                <!-- Add more columns as needed -->
            </tr>
        </thead>
        <tbody>
            <% external_jobs.forEach(job => { %>
                <tr>
                    <td><%= job.title %></td>
                    <td><%= job.company %></td>
                    <!-- Add more columns as needed -->
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
